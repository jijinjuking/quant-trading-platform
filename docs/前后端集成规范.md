# 前后端集成技术规范
## Frontend-Backend Integration Specification v1.0

---

## 🎯 **集成概述**

基于前端 `quant-backend` 项目的完整分析，制定前后端无缝集成方案。

**前端现状**: React 19 + TypeScript，专业级交易界面，功能完整但数据为模拟  
**后端目标**: Rust微服务架构，提供真实数据和交易功能  
**集成策略**: 渐进式替换，保持前端界面不变，逐步接入真实后端API  

---

## 📋 **数据类型映射**

### **前端类型 -> 后端模型**

```typescript
// 1. 市场数据类型映射
前端: MarketPair -> 后端: MarketPair
{
  symbol: string,        // BTC/USDT
  base: string,          // BTC  
  quote: string,         // USDT
  price: number,         // 64550.20
  change: number,        // 2.45 (百分比)
  volume: string,        // "1.2B"
  funding: string,       // "0.0100%"
  type: 'SPOT' | 'PERP', // 交易类型
  isHot?: boolean,       // 热门标记
  isFav?: boolean,       // 收藏标记
  isNew?: boolean        // 新币标记
}

// 2. K线数据类型映射
前端: Candle -> 后端: Kline
{
  time: number,          // 时间戳
  open: number,          // 开盘价
  high: number,          // 最高价
  low: number,           // 最低价
  close: number,         // 收盘价
  volume: number         // 成交量
}

// 3. 交易持仓类型映射
前端: Position -> 后端: Position
{
  id: string,            // 持仓ID
  symbol: string,        // 交易对
  exchange: string,      // 交易所
  type: "LONG" | "SHORT", // 多空方向
  leverage: number,      // 杠杆倍数
  entryPrice: number,    // 开仓价格
  markPrice: number,     // 标记价格
  pnl: number,          // 未实现盈亏
  pnlPercent: number,   // 盈亏百分比
  size: number          // 持仓数量
}

// 4. 交易策略类型映射
前端: Strategy -> 后端: Strategy
{
  id: number,           // 策略ID
  name: string,         // 策略名称
  pair: string,         // 交易对
  exchange: string,     // 交易所
  status: string,       // 运行状态
  profit: string,       // 收益率
  risk: string,         // 风险等级
  type: string,         // 策略类型
  confidence: number,   // 置信度
  config: object        // 策略配置
}

// 5. 订单类型映射
前端: Order -> 后端: Order
{
  id: string,           // 订单ID
  symbol: string,       // 交易对
  type: "LIMIT" | "MARKET" | "STOP", // 订单类型
  side: "BUY" | "SELL", // 买卖方向
  price: number,        // 价格
  amount: number,       // 数量
  filled: number,       // 已成交
  time: string,         // 时间
  status: "OPEN" | "FILLED" | "CANCELED" // 状态
}
```

---

## 🔄 **API接口规范**

### **1. 市场数据API**

```rust
// GET /api/v1/market/pairs
// 获取交易对列表 (匹配前端MARKET_PAIRS)
#[derive(Serialize)]
pub struct MarketPairsResponse {
    pub pairs: Vec<MarketPair>,
    pub timestamp: i64,
}

// GET /api/v1/market/ticker/:symbol  
// 获取实时价格 (匹配前端实时更新)
#[derive(Serialize)]
pub struct TickerResponse {
    pub symbol: String,
    pub price: Decimal,
    pub change: Decimal,
    pub volume: String,
    pub funding: String,
    pub timestamp: i64,
}

// GET /api/v1/market/klines/:symbol?interval=15m&limit=200
// 获取K线数据 (匹配前端generateInitialCandles)
#[derive(Serialize)]
pub struct KlinesResponse {
    pub symbol: String,
    pub interval: String,
    pub klines: Vec<Kline>,
}

// GET /api/v1/market/orderbook/:symbol?depth=20
// 获取订单簿 (匹配前端OrderBook组件)
#[derive(Serialize)]
pub struct OrderBookResponse {
    pub symbol: String,
    pub bids: Vec<[Decimal; 2]>, // [价格, 数量]
    pub asks: Vec<[Decimal; 2]>,
    pub timestamp: i64,
}
```

### **2. 交易功能API**

```rust
// POST /api/v1/trading/orders
// 下单接口 (匹配前端TradeForm)
#[derive(Deserialize)]
pub struct PlaceOrderRequest {
    pub symbol: String,
    pub side: OrderSide,
    pub order_type: OrderType,
    pub quantity: Decimal,
    pub price: Option<Decimal>,
    pub leverage: Option<u32>,
}

#[derive(Serialize)]
pub struct PlaceOrderResponse {
    pub order_id: String,
    pub status: OrderStatus,
    pub message: String,
}

// GET /api/v1/trading/positions
// 获取持仓 (匹配前端positions数组)
#[derive(Serialize)]
pub struct PositionsResponse {
    pub positions: Vec<Position>,
    pub total_equity: Decimal,
    pub unrealized_pnl: Decimal,
}

// GET /api/v1/trading/orders?status=open
// 获取订单列表
#[derive(Serialize)]
pub struct OrdersResponse {
    pub orders: Vec<Order>,
    pub total_count: u32,
}
```

### **3. 策略管理API**

```rust
// GET /api/v1/strategies
// 获取策略列表 (匹配前端STRATEGIES)
#[derive(Serialize)]
pub struct StrategiesResponse {
    pub strategies: Vec<Strategy>,
    pub total_count: u32,
}

// POST /api/v1/strategies
// 创建策略
#[derive(Deserialize)]
pub struct CreateStrategyRequest {
    pub name: String,
    pub strategy_type: String,
    pub symbol: String,
    pub exchange: String,
    pub parameters: serde_json::Value,
}

// GET /api/v1/strategies/:id/executions
// 获取策略执行记录 (匹配前端generateStrategyExecutions)
#[derive(Serialize)]
pub struct StrategyExecutionsResponse {
    pub executions: Vec<ExecutionRecord>,
    pub total_pnl: Decimal,
    pub win_rate: f64,
}
```

### **4. AI功能API (DeepSeek集成)**

```rust
// POST /api/v1/ai/analyze
// AI市场分析 (匹配前端AICopilot)
#[derive(Deserialize)]
pub struct AIAnalyzeRequest {
    pub symbol: String,
    pub timeframe: String,
    pub analysis_type: String, // "technical" | "fundamental" | "sentiment"
}

#[derive(Serialize)]
pub struct AIAnalyzeResponse {
    pub analysis: String,
    pub confidence: f64,
    pub signals: Vec<TradingSignal>,
    pub reasoning: String,
}

// POST /api/v1/ai/strategy
// AI策略生成
#[derive(Deserialize)]
pub struct AIStrategyRequest {
    pub market_conditions: String,
    pub risk_tolerance: String,
    pub capital: Decimal,
}

#[derive(Serialize)]
pub struct AIStrategyResponse {
    pub strategy_name: String,
    pub strategy_type: String,
    pub parameters: serde_json::Value,
    pub expected_return: f64,
    pub risk_score: f64,
}
```

### **5. 用户功能API**

```rust
// GET /api/v1/user/balance
// 获取账户余额 (匹配前端钱包显示)
#[derive(Serialize)]
pub struct BalanceResponse {
    pub total_equity: Decimal,      // $124,592.45
    pub available_balance: Decimal,
    pub unrealized_pnl: Decimal,    // +$3,240.50
    pub margin_used: Decimal,
    pub balances: Vec<AssetBalance>,
}

#[derive(Serialize)]
pub struct AssetBalance {
    pub asset: String,    // "USDT", "BTC"
    pub free: Decimal,
    pub locked: Decimal,
}

// GET /api/v1/user/api-keys
// API密钥管理 (匹配前端APIConnect)
#[derive(Serialize)]
pub struct ApiKeysResponse {
    pub api_keys: Vec<ApiKeyInfo>,
}

#[derive(Serialize)]
pub struct ApiKeyInfo {
    pub id: String,
    pub exchange: String,
    pub label: String,
    pub status: String,
    pub permissions: Vec<String>,
    pub created_at: String,
}
```

---

## 🔄 **WebSocket实时数据协议**

### **消息格式标准**

```typescript
// 统一WebSocket消息格式
interface WebSocketMessage {
  type: MessageType;
  data: any;
  timestamp: number;
  requestId?: string;
}

type MessageType = 
  | 'MARKET_DATA'      // 市场数据推送
  | 'ORDER_UPDATE'     // 订单状态更新  
  | 'POSITION_UPDATE'  // 持仓变化
  | 'STRATEGY_SIGNAL'  // 策略信号
  | 'AI_ANALYSIS'      // AI分析结果
  | 'SYSTEM_LOG'       // 系统日志
  | 'ERROR'            // 错误消息
  | 'HEARTBEAT';       // 心跳包
```

### **具体消息类型**

```typescript
// 1. 市场数据推送 (匹配前端实时价格更新)
{
  type: 'MARKET_DATA',
  data: {
    symbol: 'BTC/USDT',
    price: 64550.20,
    change: 2.45,
    volume: '1.2B',
    timestamp: 1703001234567
  }
}

// 2. 订单状态更新 (匹配前端订单管理)
{
  type: 'ORDER_UPDATE',
  data: {
    orderId: 'order_123',
    symbol: 'BTC/USDT',
    status: 'FILLED',
    filledQuantity: 0.5,
    avgPrice: 64550.20
  }
}

// 3. 持仓变化 (匹配前端Position组件)
{
  type: 'POSITION_UPDATE',
  data: {
    positionId: 'pos_456',
    symbol: 'BTC/USDT',
    size: 0.5,
    markPrice: 64550.20,
    unrealizedPnl: 175.10,
    pnlPercent: 0.54
  }
}

// 4. 策略信号 (匹配前端策略可视化)
{
  type: 'STRATEGY_SIGNAL',
  data: {
    strategyId: 1,
    signal: 'BUY',
    symbol: 'BTC/USDT',
    price: 64550.20,
    confidence: 0.92,
    reasoning: 'AI detected bullish pattern'
  }
}

// 5. AI分析结果 (匹配前端AICopilot)
{
  type: 'AI_ANALYSIS',
  data: {
    symbol: 'BTC/USDT',
    analysis: 'Market showing strong upward momentum...',
    confidence: 0.85,
    signals: ['BUY'],
    timestamp: 1703001234567
  }
}

// 6. 系统日志 (匹配前端LiveTerminal)
{
  type: 'SYSTEM_LOG',
  data: {
    level: 'INFO' | 'WARN' | 'ERROR' | 'AI',
    message: 'Strategy Alpha-Grid V2 executed BUY signal',
    source: 'STRATEGY_ENGINE',
    timestamp: 1703001234567
  }
}
```

---

## 🔧 **前端集成改造**

### **1. API客户端层**

```typescript
// src/api/client.ts
export class ApiClient {
  private baseURL: string;
  private wsConnection: WebSocket | null = null;

  constructor(baseURL: string = 'http://localhost:8080') {
    this.baseURL = baseURL;
  }

  // HTTP请求方法
  async get<T>(endpoint: string): Promise<ApiResponse<T>> {
    const response = await fetch(`${this.baseURL}${endpoint}`, {
      headers: {
        'Authorization': `Bearer ${this.getToken()}`,
        'Content-Type': 'application/json',
      },
    });
    return response.json();
  }

  async post<T>(endpoint: string, data: any): Promise<ApiResponse<T>> {
    const response = await fetch(`${this.baseURL}${endpoint}`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${this.getToken()}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(data),
    });
    return response.json();
  }

  // WebSocket连接管理
  connectWebSocket(onMessage: (message: WebSocketMessage) => void) {
    const wsURL = this.baseURL.replace('http', 'ws') + '/ws';
    this.wsConnection = new WebSocket(wsURL);
    
    this.wsConnection.onmessage = (event) => {
      const message = JSON.parse(event.data);
      onMessage(message);
    };

    this.wsConnection.onclose = () => {
      // 自动重连逻辑
      setTimeout(() => this.connectWebSocket(onMessage), 3000);
    };
  }

  // 订阅市场数据
  subscribeMarketData(symbols: string[]) {
    if (this.wsConnection?.readyState === WebSocket.OPEN) {
      this.wsConnection.send(JSON.stringify({
        type: 'SUBSCRIBE',
        data: { channels: ['market_data'], symbols }
      }));
    }
  }
}
```

### **2. 状态管理集成**

```typescript
// src/store/market.ts (使用Zustand)
interface MarketState {
  pairs: MarketPair[];
  currentPrice: number;
  candles: Candle[];
  orderBook: OrderBookData;
  
  // Actions
  updatePrice: (symbol: string, price: number) => void;
  updateCandles: (candles: Candle[]) => void;
  fetchMarketData: () => Promise<void>;
}

export const useMarketStore = create<MarketState>((set, get) => ({
  pairs: [],
  currentPrice: 0,
  candles: [],
  orderBook: { bids: [], asks: [] },

  updatePrice: (symbol, price) => {
    // 更新实时价格，替换前端模拟数据
    set(state => ({
      currentPrice: price,
      pairs: state.pairs.map(p => 
        p.symbol === symbol ? { ...p, price } : p
      )
    }));
  },

  updateCandles: (candles) => {
    set({ candles });
  },

  fetchMarketData: async () => {
    const api = new ApiClient();
    const response = await api.get<MarketPairsResponse>('/api/v1/market/pairs');
    if (response.success) {
      set({ pairs: response.data.pairs });
    }
  },
}));
```

### **3. 组件改造示例**

```typescript
// 改造前端MarketTicker组件，接入真实数据
export const MarketTicker: React.FC = () => {
  const { pairs, fetchMarketData } = useMarketStore();
  const [wsClient] = useState(() => new ApiClient());

  useEffect(() => {
    // 初始加载数据
    fetchMarketData();

    // 建立WebSocket连接
    wsClient.connectWebSocket((message) => {
      if (message.type === 'MARKET_DATA') {
        useMarketStore.getState().updatePrice(
          message.data.symbol, 
          message.data.price
        );
      }
    });

    // 订阅市场数据
    wsClient.subscribeMarketData(pairs.map(p => p.symbol));
  }, []);

  return (
    <div className="market-ticker">
      {pairs.map(pair => (
        <div key={pair.symbol} className="ticker-item">
          <span>{pair.symbol}</span>
          <span className={pair.change >= 0 ? 'text-up' : 'text-down'}>
            ${pair.price.toFixed(2)}
          </span>
          <span>{pair.change.toFixed(2)}%</span>
        </div>
      ))}
    </div>
  );
};
```

---

## 🚀 **集成实施步骤**

### **Step 1: 准备阶段 (Day 1)**
1. **创建API抽象层**
   - 实现ApiClient类
   - 定义所有接口类型
   - 建立WebSocket连接管理

2. **引入状态管理**
   ```bash
   npm install zustand
   ```
   - 创建market、trading、strategy、user状态store
   - 替换组件内部状态为全局状态

### **Step 2: 基础集成 (Day 2-3)**
1. **市场数据集成**
   - 替换MARKET_PAIRS为API数据
   - 接入实时价格WebSocket推送
   - 替换K线数据生成为API获取

2. **用户认证集成**
   - 实现登录/注册界面
   - JWT token管理
   - 用户信息和余额显示

### **Step 3: 交易功能集成 (Day 4-5)**
1. **交易表单集成**
   - 下单API调用
   - 订单状态实时更新
   - 持仓数据实时同步

2. **策略管理集成**
   - 策略CRUD操作
   - 策略执行记录显示
   - 策略信号可视化

### **Step 4: 高级功能集成 (Day 6-7)**
1. **AI功能集成**
   - DeepSeek API调用
   - AI分析结果展示
   - 智能策略建议

2. **实时日志集成**
   - 系统日志WebSocket推送
   - 日志分类和过滤
   - 实时终端显示

### **Step 5: 优化和测试 (Day 8-10)**
1. **性能优化**
   - WebSocket连接优化
   - 数据缓存策略
   - 组件渲染优化

2. **错误处理**
   - 网络错误处理
   - 数据验证
   - 用户友好的错误提示

3. **测试验证**
   - 功能测试
   - 性能测试
   - 用户体验测试

---

## 📊 **集成验收标准**

### **功能验收**
- [ ] 所有模拟数据替换为真实API数据
- [ ] WebSocket实时数据推送正常工作
- [ ] 交易功能完整可用 (下单、撤单、持仓管理)
- [ ] 策略管理功能完整 (创建、编辑、执行、监控)
- [ ] AI功能正常工作 (分析、建议、对话)
- [ ] 用户认证和权限控制正常

### **性能验收**
- [ ] 页面加载时间 < 2秒
- [ ] 实时数据延迟 < 100ms
- [ ] WebSocket重连机制正常
- [ ] 内存使用稳定，无泄漏
- [ ] 支持1000+并发WebSocket连接

### **用户体验验收**
- [ ] 界面响应流畅，无卡顿
- [ ] 错误提示友好明确
- [ ] 加载状态显示完整
- [ ] 离线状态处理合理
- [ ] 快捷键功能正常

---

**集成负责人**: Frontend Developer + Backend Team  
**技术指导**: Kiro AI Architect  
**完成时间**: 10个工作日  
**验收标准**: 功能完整 + 性能达标 + 用户体验优秀