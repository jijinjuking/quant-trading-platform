//! # 接口层模块
//!
//! 本文件是接口层的入口，负责导出所有接口相关的子模块。
//!
//! ## 架构位置
//! 接口层（Interface Layer）是 DDD 四层架构的最外层，负责：
//! - 接收外部请求（HTTP/gRPC/WebSocket）
//! - 请求参数验证和转换
//! - 调用应用层服务
//! - 返回响应结果

/// HTTP 接口模块
pub mod http;
