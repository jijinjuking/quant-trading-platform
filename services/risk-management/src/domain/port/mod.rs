//! # 端口定义模块 (Domain Ports)
//!
//! 本模块定义领域层的端口（trait），是六边形架构的核心。
//!
//! ## 六边形架构说明
//! 端口是领域层与外部世界的边界，定义了领域层需要的能力。
//! 具体实现由基础设施层提供（适配器模式）。
//!
//! ## 规则
//! - 只允许定义 trait
//! - 入参/出参只能是领域对象或基础类型
//! - 禁止出现 HTTP、DB、SDK 等外部类型

/// 风控仓储端口
pub mod risk_repository_port;
