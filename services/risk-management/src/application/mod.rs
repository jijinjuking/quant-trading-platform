//! # 应用层 (Application Layer)
//!
//! 应用层负责用例编排，协调领域层和基础设施层完成业务流程。
//!
//! ## 职责
//! - 接收来自接口层的请求
//! - 调用领域服务执行业务逻辑
//! - 协调多个领域对象完成复杂用例
//!
//! ## 规则
//! - 只依赖 `domain::port` 中定义的 trait（端口）
//! - 不直接依赖基础设施层的具体实现
//! - 不包含业务规则，只负责流程编排

/// 应用服务模块
pub mod service;
